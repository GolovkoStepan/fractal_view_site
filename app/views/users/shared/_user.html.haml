.user-card
  .user-avatar
    - if user.avatar.attached?
      = image_tag(url_for(user.avatar))
    - else
      = image_tag("avatar-placeholder.gif")
  .user-info
    .user-name
      = link_to(user.full_name, user_path(user))
    .user-social
      = "Email: #{user.email}"